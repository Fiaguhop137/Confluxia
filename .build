#!/bin/bash
set -e
start=$(date +%s.%N)
clang++ -std=c++23 -O0 -g0 ./assets/src/main.cpp -o ./Confluxia
printf '[Success] Compiled Confluxia in %ss! \n' "$(awk "BEGIN {printf \"%.2f\", $(date +%s.%N) - $start}")"
(nohup git sync > /dev/null 2>&1 &)